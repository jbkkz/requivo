"""Merged into `tests/test_source_form.py`'s Encoding section (#551).

Kept on disk, empty of tests, for the same reason `tests/test_boundaries.py` is: `tests/test_encoding.py`
is cited by its bare module name from thirteen places outside this file -- five under `src/`
(`streams.py`, `deterministic/__init__.py`, `services/artifacts.py`), plus `CLAUDE.md`,
`docs/compatibility.md` and six other test files -- and the narrative-reference guard requires that
name to resolve to a real module stem. See `test_source_form.py` for the actual guard.
"""
