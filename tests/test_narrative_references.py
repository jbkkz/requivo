"""Merged into `tests/test_source_form.py`'s Narrative-references section (#551).

Kept on disk, empty of tests, for the same reason `tests/test_boundaries.py` is: this file's bare
name is cited from `docs/decisions/`, `CLAUDE.md` and elsewhere outside `src/`, and #551 holds
`src/` at zero diff. See `test_source_form.py` for the actual guard.
"""
