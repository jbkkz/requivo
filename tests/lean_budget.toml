# The lean ratchet (#553): ceilings, not targets. Every value is what `scripts/prose_measure.py`
# measured on the real tree when its PR was opened, rounded up by at most 5%, and read by
# `test_the_tree_stays_within_its_lean_budget`, which reports every breach at once. Lowering one is a
# one-line PR; raising one is legal and visible, and costs the PR one sentence here saying why the
# tree needed to grow. The estate's membership is `ESTATE_FILES` in tests/test_source_form.py.
#
# Last measured 2026-09-17 on 627-t1-rules (#627, tranche 1: the rules). Tranches 2-5 lower these.

[src]
# src/requivo, .py files. Unchanged since #553: the src/ cut is tranche 2 of #627.
prose_share_max = 0.469
# Raised from 1672 to 1710 across #601's review rounds: `_prompt_perimeter_choice` is CLI-only
# terminal I/O and had nowhere else to land.
largest_module_max_lines = 1710
module_docstring_max_lines = 60
function_docstring_max_lines = 67

[tests]
# tests/, .py files. `largest_module_max_lines` excludes the meta-guard estate and is set at #555's
# own 800-line rule for an ordinary test module rather than at a 5% rounding.
ratio_max = 2.84
largest_module_max_lines = 800
module_docstring_max_lines = 78
function_docstring_max_lines = 21

[estate]
# The tests that guard the repo's own self-description rather than its runtime, summed.
total_max_lines = 4764

[docs]
compatibility_max_lines = 418
decision_record_max_lines = 485

[claude_md]
# CLAUDE.md is loaded into every session; it holds the rules and the map, and nothing else (#627).
max_lines = 125
