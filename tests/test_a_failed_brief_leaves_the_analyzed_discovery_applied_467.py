"""Renamed to `tests/test_finalize_discovery_keeps_a_paid_analyze_call.py` (#555): the old name
was an issue number, not a subject.

Kept on disk, empty of tests, because `tests/test_a_failed_brief_leaves_the_analyzed_discovery_applied_467.py`
is cited by its bare module name from `src/requivo/services/discovery.py`, which this change must not
touch. See `test_finalize_discovery_keeps_a_paid_analyze_call.py` for the actual tests.
"""
