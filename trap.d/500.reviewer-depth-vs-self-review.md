Observed while implementing #500 in jbkkz/requivo, at the self-review step (developer spine's
`agents/developer/review.md`): that phase file says unconditionally "spawn two agents against your
own committed diff, in the same message" (Explore + oss:auditor). But a jit-context reminder titled
"One reviewer per change, two only where it pays" (`reviewer-depth.md`, matched on tool `Agent` /
`oss:auditor`) fired right after I dispatched both spawns in one message, saying a second reviewer is
only warranted when the diff touches `core/`, `services/`, persistence, or a public payload shape —
listing "docs-only, CI-only or scripts-only diff gets one" as the example of the opposite case. My
diff for #500 was tests/web/test_web_security.py + docs/decisions/*.md + changelog.d/*.md — none of
the trigger paths — so by the jit-context rule's own criteria, one reviewer would have sufficed.

The reminder fired too late to act on (both agents were already launched in the same tool-call
message, per the phase file's own instruction to send them together). I did not cancel or re-spawn;
I let both proceed and noted the tension in my report.

Not sure this is a real rule conflict worth curating on its own — it may simply be that
`agents/developer/review.md`'s "always two" is deliberate (self-review against your own diff is a
narrower, higher-stakes moment than a manager's per-PR triage, so the cost/benefit measured for the
tick loop may not transfer), and the jit-context note was authored with the manager/tick loop in mind
rather than the developer's own self-review phase. But if that's the intent, the jit-context note's
`match: oss:auditor` fires unconditionally on the tool call regardless of caller, so a developer lane
following its own phase file to the letter will always see this reminder read as friction/contradiction
right after making the correct call per its own brief. Worth the curator deciding whether the note
needs a caller-scoped match, or whether developer self-review should also skip the second spawn for
a docs/test-only diff -- I did not decide this myself.
